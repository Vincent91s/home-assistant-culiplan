__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
# NOTE: the pre-built Lovelace card bundles live in
# custom_components/culiplan/frontend/cards/ (NOT a dist/ dir) so they ship
# with the HACS integration copy. They are committed deliberately.
.env
.venv/
venv/
.mypy_cache/
.coverage
htmlcov/

# Node dependencies (Lovelace card build pipeline)
node_modules/
pnpm-debug.log
.pnpm-store/

# macOS
.DS_Store
